export enum OrganizationKeys {
    ORGANIZATIONS = 'ORGANIZATIONS',
}

export enum OrganizationEndpoints {
    ORGANIZATIONS = '/v1/organizations',
}
export enum CathegoryEndpoints {
    CATHEGORIES = '/v1/organization-cathegories',
}